// Online C++ compiler to run C++ program online#include <bits/stdc++.h>using namespace std; int main(){    int t;    cin>>t;        while(t--){        int n;        cin>>n;        vector<int> arr(n);        for(int i=0;i<n;i++) cin>>arr[i];                int maxctr = 0;        int ctr = 0;                for(int i = 0; i < n; i++){            if(arr[i]==0) ctr++;            else{                maxctr = max(ctr,maxctr);                ctr=0;            }        }        maxctr = max(ctr,maxctr);        cout<<maxctr<<endl;    }    return 0;}